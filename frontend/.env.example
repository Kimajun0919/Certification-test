# Base URL of the FastAPI backend.
# In development the Vite proxy rewrites /api → http://localhost:8000,
# so leave this blank for local dev and only set it for production.
VITE_API_URL=certification-test-production.up.railway.app
